use crate::world::Chunk;
use crate::pos::ChunkPos;

//pub trait Feature<T> {
//    /// Create the feature.
//    fn new() -> T;
//    /// Generate the feature and return all of the chunkpos that it will apply to.
//    fn generate(&mut self: T) -> Vec<ChunkPos>;
//    /// Applies the feature to the chunk and removed it from the application positions.
//    fn apply_chunk(&mut self: T, chunk: &mut Chunk);
//}



